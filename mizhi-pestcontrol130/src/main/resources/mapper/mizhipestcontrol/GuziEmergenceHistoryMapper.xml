<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.nwafu.mizhipestcontrol.mapper.GuziEmergenceHistoryMapper">

    <select id="getSeedingData" resultType="cn.edu.nwafu.mizhipestcontrol.domain.GuziEmergenceHistory">
        SELECT * FROM guzi_emergence_history
        <where>
            <if test="baseId != null">
                AND base_id = #{baseId}
            </if>
        </where>
    </select>

</mapper>
